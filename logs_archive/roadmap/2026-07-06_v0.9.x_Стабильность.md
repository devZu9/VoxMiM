# v0.9.x — Стабильность *(завершена)*

- [x] **Pending.raw + transcribe без диска** *(v0.9.6)* — сырые f32+rate, no двойного ресемпла, детальный лог, таймаут 10с, pending ретраится сразу
- [x] **Pending WAV + индикатор перезапуска** *(v0.9.4)* — hourglass в трее при восстановлении
- [x] **State = Idle на первой ошибке** *(v0.9.3)* — дефолт 60s, ретраи 30s
- [x] **Таймаут whisper + retry** *(v0.9.2)* — set_read_timeout, 3 попытки с backoff, настройка в UI
- [x] **Clipboard изолирован от main loop** *(v0.9.1)* — insert_text() в отдельном потоке
- [x] **VAD threshold f32, синхронизация трея** *(v0.9.0)* — агрессивность → чувствительность, polling config.json
