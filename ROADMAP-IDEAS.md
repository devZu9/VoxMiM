# ROADMAP-IDEAS — идеи и отложенное

> Читается только по явному запросу («что в идеях?»). «Взять в работу» = перенести пункт в ROADMAP (Насущные задачи).

- [ ] **Wake Word редизайн** — скользящее окно, whisper-server small, VAD + Wake без конфликтов (попытка v0.8.0 → откат)
- [ ] **Скрытие строки заголовка окна настроек** — замена remove_window_caption() на Fenestra-опцию без caption
- [ ] **Профилирование и оптимизация времени транскрибации**
- [ ] **Библиотека для повторного использования (crate)**
- [ ] **GUI на native Windows controls вместо egui** — Fenestra 🟢 (реализовано для настроек)
- [ ] **Фоновый пинг whisper-server** — отдельный поток раз в 10с стучится к серверу; если не ответил — включает recovering и перезапускает без ожидания записи пользователя
- [ ] **Копировать последнюю фразу из трея** — пункт меню: случайно перевёл фокус → ПКМ на трее → «Копировать последнюю фразу» → в буфер обмена
- [ ] **Задержка диалога выбора папки в настройках (~6-7 с на закрытие)** — IFileDialog возвращается с задержкой ~6 с; пустая папка — та же задержка, в Блокноте мгновенно → проблема в приложении. Предположения: диалог без owner-окна + общая очередь сообщений с winit. Фикс: диалог на отдельном STA-потоке + Show(owner). Отложено — возможно, закроется переписыванием на egui
- [ ] **Переписать окно настроек (voxmim-settings) с fenestra на egui** — полный отказ от fenestra; попутно закроет задержку диалога выбора папки и костыльное скрытие заголовка
